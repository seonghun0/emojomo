spring:
  datasource:
    url: jdbc:mysql://localhost:3306/uyongseong?serverTimezone=Asia/Seoul&createDatabaseIfNotExist=true&useUnicode=true&character_set_server=utf8mb4
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace

